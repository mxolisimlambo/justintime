@model IEnumerable<WebApp.Models.SubjectsViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    model.Subject_Code
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject_Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptiveTitle)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.Stream_Code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptiveTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stream_Code)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Subject_Code }) |
            @Html.ActionLink("Details", "Details", new { id=item.Subject_Code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Subject_Code })
        </td>
    </tr>
}

</table>
